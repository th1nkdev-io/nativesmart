import 'package:flutter/material.dart';

class NsSpinner extends StatelessWidget {
  const NsSpinner({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
