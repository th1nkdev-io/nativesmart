import 'package:flutter/material.dart';

class NsDivider extends StatelessWidget {
  const NsDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return const Divider(height: 1);
  }
}
